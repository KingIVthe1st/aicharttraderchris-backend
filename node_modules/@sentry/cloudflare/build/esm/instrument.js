/**
 * Mark an object as instrumented.
 */
function markAsInstrumented(obj) {
  try {
    (obj ).__SENTRY_INSTRUMENTED__ = true;
  } catch {
    // ignore errors here
  }
}

/**
 * Check if an object is instrumented.
 */
function isInstrumented(obj) {
  try {
    return (obj ).__SENTRY_INSTRUMENTED__;
  } catch {
    return false;
  }
}

export { isInstrumented, markAsInstrumented };
//# sourceMappingURL=instrument.js.map
